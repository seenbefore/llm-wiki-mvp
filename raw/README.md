# Raw Sources

这里存放原始资料，作为整个知识库的事实输入层。

## 约束

- 只新增、归档、重命名，不做内容改写。
- 原始资料可以是 Markdown、PDF、HTML、纯文本、图片或导出的网页内容。
- 新资料应尽量保留原始标题、来源和日期信息。

## 建议子结构

- `articles/`：文章、博客、剪藏
- `papers/`：论文、报告
- `notes/`：临时摘录、人工输入
- `assets/`：图片、附件、本地化资源

如果当前还没有明确分类，也可以先直接存放，等规模增长后再细分。