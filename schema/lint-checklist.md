# Lint Checklist

每次执行 `lint` 时，至少检查以下项目：

- `wiki/index.md` 是否覆盖了新增页面
- 是否存在没有被任何页面引用的孤儿页
- 是否存在缺少来源页引用的结论
- 是否存在语义重复但名字不同的主题页、实体页、概念页
- 是否存在明显过时的总结
- 是否存在已知冲突但没有标 `冲突/待核实`
- 是否有来源页只复制原文而几乎没有结构化摘要
- 是否有分析页只停留在聊天口吻，缺少可复用结构

## 处理原则

- 能直接修复的：立即修复
- 需要更多资料的：写入对应页面的“待跟进”部分
- 涉及冲突判断的：保留双方说法并补来源，不抢跑下结论